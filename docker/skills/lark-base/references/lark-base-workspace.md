# base / workspace shortcuts

base / workspace 相关命令索引。

## 命令导航

| 文档 | 命令 | 说明 |
|------|------|------|
| `lark_get_skill(domain="base", section="base-create")` | `+base-create` | 创建 Base |
| `lark_get_skill(domain="base", section="base-get")` | `+base-get` | 获取 Base 详情 |
| `lark_get_skill(domain="base", section="base-copy")` | `+base-copy` | 复制 Base |

## 说明

- 聚合页只保留目录职责；每个命令的详细说明请进入对应单命令文档。
- `+base-*` 负责 Base 生命周期动作。
